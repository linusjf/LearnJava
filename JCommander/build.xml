<?xml version="1.0"?>
<project name="JCommander"
         default="main"
         basedir=".">
  <property file="build.properties" />
  <property file="../globalbuild.properties" />
  <property file="${cs.properties}" />
  <property file="${gformat.properties}" />
  <property file="../pmd.properties" />
  <!-- Sets variables which can later be used. -->
  <!-- The value of a property is accessed via ${} -->
  <property name="src.dir"
            location="." />
  <property name="build.dir"
            location="." />
  <property name="dist.dir"
            location="dist" />
  <property name="docs.dir"
            location="docs" />
  <loadfile property="cs.cp"
            srcFile="../${cs.classpath.file}" />
  <loadfile property="pmd.cp"
            srcFile="../${pmd.classpath.file}" />
  <loadfile property="compilerclasspath"
            srcFile="${compile.cp}" />
  <loadfile property="runclasspath"
            srcFile="${run.cp}" />
  <taskdef resource="${cs.taskdef.resource}"
           classpath="${cs.cp}" />
  <taskdef name="pmd"
           classname="net.sourceforge.pmd.ant.PMDTask"
           classpath="${pmd.cp}" />
  <!-- Deletes the existing build, docs and dist directory-->
  <target name="clean">
    <delete>
      <fileset dir="."
               includes="**/*.class" />
    </delete>
    <delete dir="${docs.dir}" />
    <delete dir="${dist.dir}" />
  </target>
  <!-- Creates the  build, docs and dist directory-->
  <target name="makedir">
    <mkdir dir="${docs.dir}" />
    <mkdir dir="${dist.dir}" />
  </target>
  <!-- Compiles the java code (including the usage of library for JUnit -->
  <target name="compile"
          depends="clean, makedir,format,checkstyle">
    <javac includeantruntime="false"
           srcdir="${src.dir}"
           destdir="${build.dir}"
           classpath="${compilerclasspath}">
      <compilerarg value="${javac.params}" />
      <compilerarg value="${javac.params1}" />
    </javac>
  </target>
  <!-- Creates Javadoc -->
  <target name="docs"
          depends="compile">
    <javadoc packagenames="${packages}"
             sourcepath="${src.dir}"
             destdir="${docs.dir}"
             additionalparam="${javadoc.params}"
             classpath="${compilerclasspath}"></javadoc>
  </target>
  <target name="manifest">
    <tstamp />
    <exec executable="bash"
          outputproperty="build">
      <arg value="-c" />
      <arg value="git log --pretty=format:'%h' | head -1" />
    </exec>
    <manifest mode="update"
              file="${jar.manifest}">
      <attribute name="Built-By"
                 value="${user.name}" />
      <section name="common">
        <attribute name="Specification-Title"
                   value="${ant.project.name}" />
        <attribute name="Specification-Version"
                   value="${version}" />
        <attribute name="Specification-Vendor"
                   value="" />
        <attribute name="Implementation-Title"
                   value="" />
        <attribute name="Implementation-Version"
                   value="${build} ${TODAY}" />
        <attribute name="Implementation-Vendor"
                   value="" />
      </section>
      <attribute name="Main-Class"
                 value="${main.class}" />
    </manifest>
  </target>
  <!--Creates the deployable jar file  -->
  <target name="jar"
          depends="compile,manifest">
    <jar destfile="${dist.dir}\${ant.project.name}-${version}.jar"
         basedir="${build.dir}"
         includes="**/*.class,**/*.properties"
         manifest="${jar.manifest}"></jar>
  </target>
  <target name="setupclasspath">
    <description>Run target</description>
    <exec executable="bash">
      <arg value="./setupclasspath" />
    </exec>
  </target>
  <target name="run">
    <description>Run target</description>
    <java classname="${main.class}"
          fork="true">
      <arg line="--help -log 2 -groups unit1,unit2,unit3 -debug -Doption=value a b c" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
        <fileset dir="${build.dir}">
          <include name="**/jcommander.properties" />
        </fileset>
      </classpath>
    </java>
    <java classname="${main.class}"
          fork="true">
      <arg line="-log 2 -groups unit1,unit2,unit3 -debug -Doption=value a b c" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
        <fileset dir="${build.dir}">
          <include name="**/jcommander.properties" />
        </fileset>
      </classpath>
    </java>
    <java classname="${main.class}"
          fork="true">
      <arg line="-log 3 -groups unit1,unit2,unit3 -debug -Doption=value a b c" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
        <fileset dir="${build.dir}">
          <include name="**/jcommander.properties" />
        </fileset>
      </classpath>
    </java>
    <java classname="${main.class}"
          fork="true">
      <arg line="-log 2 -groups unit1,unit2,unit4 -debug -Doption=value a b c" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
        <fileset dir="${build.dir}">
          <include name="**/jcommander.properties" />
        </fileset>
      </classpath>
    </java>
    <java classname="${main.class}"
          fork="true">
      <arg line="-log 2 -groups unit1,unit2,unit3 -debug -Doption=value a b d" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
        <fileset dir="${build.dir}">
          <include name="**/jcommander.properties" />
        </fileset>
      </classpath>
    </java>
    <java classname="${main.class}"
          fork="true">
      <arg line="@params.txt" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
        <fileset dir="${build.dir}">
          <include name="**/jcommander.properties" />
        </fileset>
      </classpath>
    </java>
    <java classname="${main.class}"
          fork="true">
      <arg line="@params.txt -log 2 -groups unit1,unit2,unit3 -debug -Doption=value a b d" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
        <fileset dir="${build.dir}">
          <include name="**/jcommander.properties" />
        </fileset>
      </classpath>
    </java>
    <java classname="${main.class}"
          fork="true">
      <arg line="-log 2 -groups unit1,unit2,unit3 -Doption=value a b c" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
        <fileset dir="${build.dir}">
          <include name="**/jcommander.properties" />
        </fileset>
      </classpath>
    </java>
    <java classname="${main.class}"
          fork="true">
      <arg line="-log 2 -groups unit1,unit2,unit3 -debug -Doption=val a b c" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
        <fileset dir="${build.dir}">
          <include name="**/jcommander.properties" />
        </fileset>
      </classpath>
    </java>
    <java classname="${main.class1}">
      <arg line="-l 512 -p 2" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
      </classpath>
    </java>
    <java classname="${main.class1}">
      <arg line="--length 512 --pattern 2" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
      </classpath>
    </java>
    <java classname="${main.class1}">
      <arg line="-host host1 --length 512 --pattern 2 -host host2" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
      </classpath>
    </java>
    <java classname="${main.class2}"
          inputstring="This is the password"
          fork="true">
      <arg line="-password" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
      </classpath>
    </java>
    <java classname="${main.class3}">
      <arg line="-file password.txt -files build.xml,build.properties,.main" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
      </classpath>
    </java>
    <java classname="${main.class4}">
      <arg line="-hostport example.com:8080" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
      </classpath>
    </java>
    <java classname="${main.class5}">
      <arg line="-files build.xml,build.properties,.main" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
      </classpath>
    </java>
    <java classname="${main.class6}">
      <arg line="-files build.xml;build.properties;.main" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
      </classpath>
    </java>
    <java classname="${main.class7}">
      <arg line="-age 25" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
      </classpath>
    </java>
    <java classname="${main.class7}">
      <arg line="-age 0" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
      </classpath>
    </java>
    <java classname="${main.class7}">
      <arg line="-age -10" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
      </classpath>
    </java>
    <java classname="${main.class8}">
      <arg line="-debug 3 build.xml .main" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
      </classpath>
    </java>
    <java classname="${main.class9}">
      <arg line="-level=42" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
      </classpath>
    </java>
    <java classname="${main.class10}">
      <arg line="-master master -slave slave" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
      </classpath>
    </java>
    <java classname="${main.class11}">
      <arg line="-pairs build.xml build.properties" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
      </classpath>
    </java>
    <java classname="${main.class11}">
      <arg line="-pairs build.xml build.properties .main" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
      </classpath>
    </java>
    <java classname="${main.class12}">
      <arg line="-foo build.xml build.properties" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
      </classpath>
    </java>
    <java classname="${main.class12}">
      <arg line="-foo build.xml build.properties .main" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
      </classpath>
    </java>
    <java classname="${main.class13}">
      <arg line="-mv from to" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
      </classpath>
    </java>
    <java classname="${main.class13}">
      <arg line="-mv to from" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
      </classpath>
    </java>
    <java classname="${main.class14}">
      <arg line="-verbose commit --amend --author=cbeust A.java B.java" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
      </classpath>
    </java>
    <java classname="${main.class14}">
      <arg line="-verbose add -i *.java" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
      </classpath>
    </java>
    <java classname="${main.class15}">
      <arg line="-v -port 1234" />
      <classpath>
        <pathelement location="${dist.dir}\${ant.project.name}-${version}.jar" />
        <pathelement path="${java.class.path}" />
        <pathelement path="${runclasspath}" />
      </classpath>
    </java>
  </target>
  <target name="format">
    <fileset dir="${basedir}"
             id="javasrcs">
      <include name="**/*.java" />
    </fileset>
    <pathconvert property="sources"
                 refid="javasrcs"
                 pathsep=" " />
    <loadfile property="gformat.cp"
              srcFile="../${gformat.classpath.file}" />
    <java classname="${gformat.main.class}">
      <arg line="-i ${sources}" />
      <classpath>
        <pathelement path="${gformat.cp}" />
        <pathelement path="${java.class.path}" />
      </classpath>
    </java>
    <exec executable="bash">
      <arg line="../cformat ${basedir}" />
    </exec>
    <exec executable="bash">
      <arg line="../xformatall ${basedir}" />
    </exec>
  </target>
  <target name="checkstyleg">
    <fileset dir="${basedir}"
             id="javasrcs">
      <include name="**/*.java" />
    </fileset>
    <pathconvert property="sources"
                 refid="javasrcs"
                 pathsep=" " />
    <loadfile property="cs.cp"
              srcFile="../${cs.classpath.file}" />
    <java classname="${cs.main.class}"
          logError="true">
      <arg line="-c ../${cs.config} -p ${cs.properties} -o ${ant.project.name}-xpath.xml -g ${sources}" />
      <classpath>
        <pathelement path="${cs.cp}" />
        <pathelement path="${java.class.path}" />
      </classpath>
    </java>
  </target>
  <target name="checkstyle">
    <first id="checkstylefile">
      <fileset dir=".."
               includes="${cs.config}" />
    </first>
    <checkstyle config="${toString:checkstylefile}"
                failOnViolation="false"
                properties="${cs.properties}">
      <fileset dir="${src.dir}"
               includes="**/*.java" />
      <formatter type="plain" />
      <formatter type="plain"
                 toFile="${cs.output}" />
      <classpath>
        <pathelement path="${cs.cp}" />
        <pathelement path="${java.class.path}" />
      </classpath>
    </checkstyle>
  </target>
  <target name="pmd">
    <pmd cacheLocation="${pmd.cacheFile}"
         minimumPriority="${pmd.minimumPriority}"
         suppressMarker="${pmd.suppressMarker}"
         failuresPropertyName="pmd.errCount">
      <ruleset>../${pmd.config}</ruleset>
      <auxclasspath>
        <pathelement path="${basedir}" />
      </auxclasspath>
      <formatter type="${pmd.reportFormat}"
                 toFile="${pmd.output}"
                 toConsole="${pmd.toConsole}"></formatter>
      <fileset dir="${basedir}">
        <include name="**/*.java" />
      </fileset>
    </pmd>
  </target>
  <target name="pmd-result"
          depends="pmd"
          if="pmd.errCount">
    <echo message="${pmd.errCount} violations found." />
  </target>
  <target name="main"
          depends="compile, jar, docs,pmd-result">
    <description>Main target</description>
  </target>
</project>
